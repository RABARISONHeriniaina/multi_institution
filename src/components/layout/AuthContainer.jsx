const AuthContainer = ({ children }) => (
  <div className="min-h-screen bg-white from-orange-50 to-yellow-50 flex items-center justify-center p-4">
    {children}
  </div>
);

export default AuthContainer;
